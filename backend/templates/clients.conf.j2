# NAS clients are loaded from the SQL 'nas' table at runtime via
# read_clients = yes in mods-enabled/sql.  Do NOT list them here too
# or FreeRADIUS will refuse to start with "duplicate client" errors.
client localhost {
  ipaddr    = 127.0.0.1
  secret    = {{ radius_shared_secret }}
  shortname = localhost
}
